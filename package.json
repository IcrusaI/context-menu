{
  "name": "context-menu",
  "version": "0.1.0",
  "description": "Allows you to open the context menu by the cursor location.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prettier": "npx prettier --write src/* test/* example/*",
    "tslint": "tslint -p tslint -p tsconfig.json",
    "codestyle": "npm run prettier && npm run tslint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run codestyle"
    }
  },
  "keywords": [],
  "author": "crusa",
  "license": "MIT",
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^3.1.0",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typedoc": "^0.25.3",
    "typescript": "^5.2.2"
  }
}
